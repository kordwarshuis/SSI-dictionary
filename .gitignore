node_modules/
.nuxt/
.output/
dist/
public/

# public/data/ is generated at CI runtime and deployed directly to gh-pages.
# It is never committed to main.

# Local env overrides – never commit the real file
.env

# OS artefacts
.DS_Store
Thumbs.db
